{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Renderer receives core IPC only; sidecar authority remains in Rust.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
